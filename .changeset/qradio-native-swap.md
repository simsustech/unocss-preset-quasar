---
'@simsustech/unocss-preset-quasar': patch
---

QRadio Phase 4: replaced custom SVG visual with visible native `<input type="radio">` themed via `accent-color`. Stripped the dead `__bg`, `__check`, `__icon-container`, `__icon` shortcut classes.
