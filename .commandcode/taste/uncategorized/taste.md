# Uncategorized

- When debugging errors, attribute them to the actual source (check stack traces, patch points) rather than the nearest visible tool in the user's stack. Verify the cause before naming it. Confidence: 0.70
